
.main
  .nav.d-flex.justify-content-center.text-center.py-2.border-bottom
    .categorie.w-25
      = link_to 'ALL','#',{class: 'text-muted h4'}
    .categorie.w-25
      = link_to 'MEN','#',{class: 'text-muted h4'}
    .categorie.w-25
      = link_to 'WOMEN','#',{class: 'text-muted h4'}

  .container
    .card-deck.card-deck1.mt-4
      .card.h-100
        = link_to image_tag(@coordinates.last.image&.url, class:"card-img"), coordinate_path(@coordinates.pop&.id) if @coordinates.present?
      .card.h-100
        = link_to image_tag(@coordinates.last.image&.url, class:"card-img"), coordinate_path(@coordinates.pop&.id) if @coordinates.present?
      .card.h-100
        = link_to image_tag(@coordinates.last.image&.url, class:"card-img"), coordinate_path(@coordinates.pop&.id) if @coordinates.present?
    .card-deck.card-deck2.mt-4
      .card.h-100
        = link_to image_tag(@coordinates.last.image&.url, class:"card-img"), coordinate_path(@coordinates.pop&.id) if @coordinates.present?
      .card.h-100
        = link_to image_tag(@coordinates.last.image&.url, class:"card-img"), coordinate_path(@coordinates.pop&.id) if @coordinates.present?